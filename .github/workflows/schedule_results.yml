name: Schedule Results

on:
  schedule:
    - cron: "5 * * * *"  # Every hour at :05 past — covers any results scheduled on the hour
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Run scheduled results
        run: go run ./cmd/scheduleresults
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          SLACK_ADMIN_USER_ID: ${{ secrets.SLACK_ADMIN_USER_ID }}

      - name: Commit results state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add polls/_results_state.json
          git diff --cached --quiet && exit 0
          git commit -m "chore: update results state [skip ci]"
          git pull --rebase
          git push
